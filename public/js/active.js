let color = '#852EFF';
let bgColor = 'rgba(0, 0, 0, .3)';

(function ($) {
  // "use strict";

  $('.html').rProgressbar({
    percentage: 95,
    fillBackgroundColor: color,
    backgroundColor: bgColor,
  });
  $('.framework').rProgressbar({
    percentage: 90,
    fillBackgroundColor: color,
    backgroundColor: bgColor,
  });

  $('.js').rProgressbar({
    percentage: 90,
    fillBackgroundColor: color,
    backgroundColor: bgColor,
  });

  $('.ts').rProgressbar({
    percentage: 85,
    fillBackgroundColor: color,
    backgroundColor: bgColor,
  });

  $('.react').rProgressbar({
    percentage: 95,
    fillBackgroundColor: color,
    backgroundColor: bgColor,
  });

  $('.native').rProgressbar({
    percentage: 85,
    fillBackgroundColor: color,
    backgroundColor: bgColor,
  });

  $('.next').rProgressbar({
    percentage: 90,
    fillBackgroundColor: color,
    backgroundColor: bgColor,
  });

  $('.node').rProgressbar({
    percentage: 85,
    fillBackgroundColor: color,
    backgroundColor: bgColor,
  });

  $('.redux').rProgressbar({
    percentage: 85,
    fillBackgroundColor: color,
    backgroundColor: bgColor,
  });

  $('.database').rProgressbar({
    percentage: 85,
    fillBackgroundColor: color,
    backgroundColor: bgColor,
  });

  $('.oauth').rProgressbar({
    percentage: 85,
    fillBackgroundColor: color,
    backgroundColor: bgColor,
  });

  $('.tools').rProgressbar({
    percentage: 90,
    fillBackgroundColor: color,
    backgroundColor: bgColor,
  });
})(jQuery);
